<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee 
                             https://jakarta.ee/xml/ns/jakartaee/web-app_5_0.xsd"
         version="5.0">

  <display-name>TravelMate Backend API</display-name>

  <!-- ================= CORS FILTER ================= -->
  <!-- MUST be first -->
  <filter>
    <filter-name>CorsFilter</filter-name>
    <filter-class>com.travelmate.filter.CorsFilter</filter-class>
  </filter>

  <filter-mapping>
    <filter-name>CorsFilter</filter-name>
    <url-pattern>/*</url-pattern>
  </filter-mapping>

  <!-- ================= JWT FILTER ================= -->
  <filter>
    <filter-name>JwtFilter</filter-name>
    <filter-class>com.travelmate.config.JwtFilter</filter-class>
  </filter>

  <!-- Secure all APIs -->
  <filter-mapping>
    <filter-name>JwtFilter</filter-name>
    <url-pattern>/api/*</url-pattern>
  </filter-mapping>

  <!-- ================= AUTH ================= -->
  <servlet>
    <servlet-name>AuthServlet</servlet-name>
    <servlet-class>com.travelmate.controller.auth.AuthServlet</servlet-class>
  </servlet>

  <servlet-mapping>
    <servlet-name>AuthServlet</servlet-name>
    <url-pattern>/api/auth/*</url-pattern>
  </servlet-mapping>

  <!-- ================= PROFILE ================= -->
  <servlet>
    <servlet-name>UserProfileServlet</servlet-name>
    <servlet-class>com.travelmate.controller.profile.UserProfileServlet</servlet-class>
  </servlet>

  <servlet-mapping>
    <servlet-name>UserProfileServlet</servlet-name>
    <url-pattern>/api/profile/*</url-pattern>
  </servlet-mapping>

  <!-- ================= MATCHING ================= -->
  <servlet>
    <servlet-name>MatchingServlet</servlet-name>
    <servlet-class>com.travelmate.controller.matching.MatchingServlet</servlet-class>
  </servlet>

  <servlet-mapping>
    <servlet-name>MatchingServlet</servlet-name>
    <url-pattern>/api/matches/*</url-pattern>
  </servlet-mapping>

  <!-- ================= CHAT API ================= -->
  <servlet>
    <servlet-name>ChatServlet</servlet-name>
    <servlet-class>com.travelmate.controller.chat.ChatServlet</servlet-class>
  </servlet>

  <servlet-mapping>
    <servlet-name>ChatServlet</servlet-name>
    <url-pattern>/api/chat/*</url-pattern>
  </servlet-mapping>

  <!-- ================= GROUP ================= -->

<servlet>
    <servlet-name>GroupServlet</servlet-name>
    <servlet-class>com.travelmate.controller.group.GroupServlet</servlet-class>
  </servlet>

  <servlet-mapping>
    <servlet-name>GroupServlet</servlet-name>
    <url-pattern>/api/group/*</url-pattern>
  </servlet-mapping>

  <!-- ================= SESSION ================= -->
  <session-config>
    <session-timeout>30</session-timeout>
    <cookie-config>
      <http-only>true</http-only>
    </cookie-config>
  </session-config>





</web-app>
